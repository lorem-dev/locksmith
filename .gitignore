bin/
# gen/proto/ is committed - required for library consumers via go get
docs/superpowers/
.worktrees/
.reports/
# Release artifact staging directory (rendered install.sh, zips, checksums).
dist/
*.sock
TODO.md
# Track local skills but nothing else under .claude/
.claude/*
!.claude/skills/
.agents/*
# Output files
*.out
# Generated bundle artefacts (built by make build-all / make init).
internal/bundled/assets/
