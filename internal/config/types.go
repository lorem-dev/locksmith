package config

// Vault type constants identify supported vault backends.
const (
	VaultKeychain     = "keychain"
	VaultGopass       = "gopass"
	VaultOnePassword  = "1password"
	VaultGnomeKeyring = "gnome-keyring"
)
